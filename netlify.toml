[build]
  publish = "."
  functions = "netlify/functions"

# Nice alias so the site can also call /api/ask if you prefer
[[redirects]]
  from = "/api/ask"
  to = "/.netlify/functions/ask"
  status = 200
